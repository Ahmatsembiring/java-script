const names=["ahmat","Pra","Yoga","Sembiring"];
const firstName = names[0];
const middelName= names[1];
const lastNames = names[0];




console.info(firstName)
console.info(middelName)
console.info(lastNames)